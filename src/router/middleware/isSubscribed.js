export default function isSubscribed ({ next }){
    // if(!store.getters.auth.isSubscribed){
    //     return next({
    //        name: 'dashboard'
    //     })
    // }
   
    return next()
   }